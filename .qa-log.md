# Stick Fight QA Log

Append-only record of every QA agent run. Most recent at the bottom.

Format per entry:
```
## YYYY-MM-DD HH:MM — pass/fail summary
- details
- fixes applied / open issues
```

## 2026-04-14 (time unknown) — BLOCKED 0/16
- Unable to execute QA checklist: permission denied for `mcp__Claude_Preview__preview_*` tools and `Bash` (needed to start preview server, drive the game via preview_eval, and fetch ISO timestamp).
- No code was changed. No regressions investigated (static-only review not performed because task required live runtime verification per checklist items 1-16).
- Open: caller needs to grant preview tool + Bash permissions, or run the checklist in a harness where they are allowed. Re-dispatch the QA agent once permissions are available.

## 2026-04-15 08:16 UTC — PASS 16/16 (baseline)
- Boot ✅ (items 1-2): no console errors, all globals resolved.
- Menu flow ✅ (3-5): Aurora P1, CPU picks Crimson P2, Twilight stage, state→playing.
- Loop ✅ (6) — BUT native requestAnimationFrame is throttled to 0 Hz in the headless preview. `document.hidden=false` patch alone is NOT sufficient. setTimeout-based RAF shim also gets throttled to ~1 Hz after ~30s. **MessageChannel-based RAF shim** was required for stable timing; recommend adding this to the QA workflow notes in QA.md.
- Inputs ✅ (7-9): 'd' moves +26px/300ms, 'f' → light attack, 'w' → jump (-13y). CPU must be neutralized (`window.cpuInput = () => makeInput()`) — otherwise Crimson's AI attacks through every harness pause and corrupts state checks.
- Combat ✅ (10-12): heavy lands at stateTime=10 for −14 HP; ult fires, ult meter drains; throw locks victim at stateTime=3 (beingThrown=29).
- Ringout + replay ✅ (13-14): match-point heavy near edge → `state==='ringout'` transient → `matchover`; mid-stage KO → `state==='replay'`. Note: replay is skipped if replayBuffer.length < 30 (replay.js:22), so tests firing a KO immediately after resetRound can false-negative — warm up ~1s first.
- UI ✅ (15-16): Escape toggles `paused` with pause menu; 'm' toggles Audio.isMuted both directions. Audio starts muted due to browser autoplay policy — asserting strict `===true` on first press can false-negative; assert toggle instead.
- Open questions from dev brief:
  - **Body-physics/throw/ult visual smoothness**: NOT visually assessed — only state transitions verified. Needs a human or screenshot sweep.
  - **All 4 characters distinct**: all 4 cards render with distinct glow CSS vars; only Aurora was played. Didn't spot-check Obsidian/Verdant.
  - **Announcer timing**: observed correct sequence ("ROUND 1/FIGHT!/AURORA STORM!/FLAWLESS VICTORY!/REPLAY") but gap timing not audited.
- Minor oddity (not a regression): p2.hp drops from 100→92 during the "ROUND 1 / FIGHT!" intro even with cpuInput stubbed. ~8 dmg from an unknown source before the fighters are under player control. Worth a glance from dev — possibly a stray hitbox active during intro, or particle/shake test-fire.
- No code was changed. No commits/pushes.
